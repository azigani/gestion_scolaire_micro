apiVersion: apps/v1
kind: Deployment
metadata:
  name: edumanager-financial
  namespace: edumanager
spec:
  replicas: 2
  selector:
    matchLabels:
      app: edumanager-financial
  template:
    metadata:
      labels:
        app: edumanager-financial
    spec:
      containers:
      - name: edumanager-financial
        image: edumanager/financial:latest
        ports:
        - containerPort: 8084
        env:
        - name: SPRING_PROFILES_ACTIVE
          valueFrom:
            configMapKeyRef:
              name: edumanager-config
              key: SPRING_PROFILES_ACTIVE
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: edumanager-secret
              key: POSTGRES_PASSWORD
        - name: SPRING_REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: edumanager-secret
              key: REDIS_PASSWORD
        - name: EDUMANAGER_JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: edumanager-secret
              key: JWT_SECRET
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8084
          initialDelaySeconds: 60
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8084
          initialDelaySeconds: 30
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: edumanager-financial
  namespace: edumanager
spec:
  selector:
    app: edumanager-financial
  ports:
  - port: 8084
    targetPort: 8084
  type: ClusterIP
