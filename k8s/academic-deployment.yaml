apiVersion: apps/v1
kind: Deployment
metadata:
  name: edumanager-academic
  namespace: edumanager
spec:
  replicas: 2
  selector:
    matchLabels:
      app: edumanager-academic
  template:
    metadata:
      labels:
        app: edumanager-academic
    spec:
      containers:
      - name: edumanager-academic
        image: edumanager/academic:latest
        ports:
        - containerPort: 8083
        env:
        - name: SPRING_PROFILES_ACTIVE
          valueFrom:
            configMapKeyRef:
              name: edumanager-config
              key: SPRING_PROFILES_ACTIVE
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: edumanager-secret
              key: POSTGRES_PASSWORD
        - name: SPRING_REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: edumanager-secret
              key: REDIS_PASSWORD
        - name: EDUMANAGER_JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: edumanager-secret
              key: JWT_SECRET
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8083
          initialDelaySeconds: 60
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8083
          initialDelaySeconds: 30
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: edumanager-academic
  namespace: edumanager
spec:
  selector:
    app: edumanager-academic
  ports:
  - port: 8083
    targetPort: 8083
  type: ClusterIP
